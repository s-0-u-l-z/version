## v1.2.0 - 2025-11-05
### Added
* Implemented new `fetchAndSplitChangelog` function in `main.js`.
* Added three new content divs to `index.html` to hold the split changelog parts.

### Fixed
* Resolved an issue where `marked.parse` was throwing an error with non-string input.

## v1.1.0 - 2025-10-28
### Changed
* Updated the sidebar icon style in `style.css`.
* Refactored tab switching logic in `main.js` for better performance.

### Fixed
* Corrected a layout bug affecting the window controls (`.win-ctrls`).

## v1.0.0 - 2025-10-20
* Initial release of the application.
